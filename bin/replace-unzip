#!/bin/dash

ZIP="$1"
TO="$2"

exec docker run --rm --user 1000 \
   -v /home/frew/code/dotfiles/perl/replace-unzip:/opt/replace-unzip \
   -v $(realpath $ZIP):/opt/$(basename $ZIP) \
   -v $(realpath $TO):$(realpath $TO) \
   frew/perl /opt/replace-unzip /opt/$(basename $ZIP) /opt/to
